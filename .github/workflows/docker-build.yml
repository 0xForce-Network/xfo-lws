name: Build+Push Daemon/Admin Docker Image

on:
  push:
    branches: [ "master" ]

jobs:
  build_and_push:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build Docker image
      run: docker build --no-cache --tag vtnerd/xfo-lws:master .
    - name: Add additional tags
      run: docker image tag vtnerd/xfo-lws:master ghcr.io/vtnerd/xfo-lws:master

    - name: Login to GitHub Container Registry
      uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    - name: Push to GitHub Container Registry
      run: docker push -a ghcr.io/vtnerd/xfo-lws

    - name: Login to Docker.io
      uses: docker/login-action@v3
      with:
        username: ${{ github.actor }}
        password: ${{ secrets.DOCKER_VTNERD_TOKEN }}
    - name: Push to Docker.io
      run: docker push -a vtnerd/xfo-lws
